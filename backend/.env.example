# Database Configuration
POSTGRES_DB=sporthub
POSTGRES_USER=admin
POSTGRES_PASSWORD=secret

# Security
# Run `openssl rand -base64 32` to generate a strong secret
JWT_SECRET=my-super-secret-key-for-local-development-only

# RabbitMQ Configuration
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
