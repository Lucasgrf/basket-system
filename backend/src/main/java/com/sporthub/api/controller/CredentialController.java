package com.sporthub.api.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * TODO (Phase 3): Remove this controller. Credential entity is deleted.
 * Credential concept is replaced by Athlete/Technician profiles.
 * Keeping as empty stub to avoid breaking route scanning.
 */
@RestController
@RequestMapping("/credentials")
public class CredentialController {
}
